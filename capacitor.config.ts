import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.enric.app',
  appName: 'Enric App',
  webDir: 'dist/enric-app/browser'
};

export default config;
